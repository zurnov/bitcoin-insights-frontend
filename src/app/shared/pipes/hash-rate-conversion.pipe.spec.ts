import { HashRateConversionPipe } from './hash-rate-conversion.pipe';

describe('HashRateConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new HashRateConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
