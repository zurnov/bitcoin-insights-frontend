import { DifficultyConversionPipe } from './difficulty-conversion.pipe';

describe('DifficultyConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new DifficultyConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
